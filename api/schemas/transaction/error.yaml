type: object
properties:
  error:
    type: string
  code:
    type: string
    enum: [USER_NOT_FOUND, ACCOUNT_NOT_FOUND, INVALID_TRANSACTION_DATA, ERROR_UPDATE_TRANSACTION, ERROR_CREATE_TRANSACTION, ERROR_DELETE_TRANSACTION]
    